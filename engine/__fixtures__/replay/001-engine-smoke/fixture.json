{
  "name": "001-engine-smoke",
  "description": "Proves the replay loop end to end: an input triple folds into state and a transcript that are recorded and compared byte for byte. The shared fixture cartridge is written with deliberately unsorted keys, so a regression in canonical key ordering fails here rather than in a subtle way later, and it goes through the loader on the way in, so a world that stops validating fails here too.",
  "seed": "2026-08-05/0/deep-foundation",
  "cartridge": "minimal",
  "events": [
    {
      "type": "session.start"
    },
    {
      "type": "shell.exec",
      "payload": {
        "input": "pwd"
      }
    },
    {
      "type": "shell.exec",
      "payload": {
        "input": "ls -la"
      }
    },
    {
      "type": "session.end"
    }
  ]
}
